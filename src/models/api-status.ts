export enum APIStatus {
  LOADING = 'loading',
  ERROR = 'failed',
  IDLE = 'idle',
}

export enum AuthStatus {
  LOADING = 'loading',
  ERROR = 'failed',
  SUCCESS = 'idle',
  NOT_USED = 'notUsed',
}
